package xinrun.sunshine.customer.facade.impl;

import xinrun.sunshine.customer.facade.PremiseFacade;
import xinrun.sunshine.utils.ResultData;

/**
 * Created by sunshine on 4/6/15.
 */
public class PremiseFacadeImpl implements PremiseFacade {

    @Override
    public ResultData queryPremise(Integer premiseId) {
        ResultData result = new ResultData();

        return result;
    }

    @Override
    public ResultData queryPremiseQRCode(Integer premiseId) {
        ResultData result = new ResultData();

        return result;
    }
}
